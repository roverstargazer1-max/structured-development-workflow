interface:
  display_name: "结构化开发规划与文档治理"
  short_description: "先澄清真实背景，再维护可追踪文档包和独立开发任务卡"
  default_prompt: "Use $structured-development-workflow to inspect and clarify project facts, then maintain an indexed, cross-linked document package with separate task cards, return only artifact links and status, and stop before code changes or agent delegation."

policy:
  allow_implicit_invocation: true
